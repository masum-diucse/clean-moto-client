import React from 'react';
import { CardElement } from '@stripe/react-stripe-js';

const SinglePayment = () => {

  return (
    <div>
      <CardElement />
    </div>
  );
};

export default SinglePayment;